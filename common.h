#pragma once

#include <cstdio>
#include <cstdlib>
#include <cassert>
#include <algorithm>
#include <iostream>

using namespace std;

#define E(x) { cerr << #x << " = " << (x) << "   "; }
#define Eo(x) { cerr << #x << " = " << (x) << endl; }
#define EO(x) Eo(x)

typedef unsigned char uchar;

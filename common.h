#pragma once

#include <cstdio>
#include <cstdlib>
#include <cstdlib>
#include <cstring>
#include <cassert>
#include <algorithm>
#include <iostream>
#include <fstream>
#include <cmath>

using namespace std;

#define E(x) { cerr << #x << " = " << (x) << "   "; }
#define Eo(x) { cerr << #x << " = " << (x) << endl; }
#define EO(x) Eo(x)

typedef unsigned char uchar;
